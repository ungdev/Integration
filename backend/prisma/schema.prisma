generator client {
  provider           = "prisma-client"
  output             = "../src/prisma/build"
  moduleFormat       = "cjs"
  importFileExtension = ""
}

datasource db {
  provider = "postgresql"
}
